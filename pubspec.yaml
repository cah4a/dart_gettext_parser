name: gettext_parser
description: 'Parse and compile gettext po and mo files with dart'
version: 0.0.1

environment:
  sdk: ">=2.0.0 <3.0.0"

dev_dependencies:
  test: ^1.3.4
  collection: ^1.14.11
  path: ^1.6.2
